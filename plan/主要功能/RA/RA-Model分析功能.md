
---

## 一、这个 RA – Model 模块的核心定位（一句话版）

**在给定 Segment + ODM 的业务上下文下，对具体机型（Model / Mode）进行 RA 表现分析，结合趋势、机型间对比，以及来自 RA DETAIL 的 Top 问题类型统计，帮助用户判断“这个机型的 RA 问题是高不高、是不是持续的、主要集中在哪些问题上”。**

---

## 二、用户真实的使用路径（你描述的“正确使用方式”）

### 1️⃣ 筛选顺序是有强引导的（不是随便选）

用户的**自然操作路径**是：

1. 进入 **RA → Model Analysis**
2. 选择 **时间范围**

   * 默认最近 6 个月
3. **先选择 Segment**

   * 用来限定业务范围
4. **再选择 ODM**

   * 在已选 Segment 下进一步收敛
5. **再选择 Model（Mode / 机型）**

   * Model 是在「Segment + ODM」条件下**真实存在的机型集合**
   * Model 选择控件：

     * 支持 **多选**
     * 支持 **关键词搜索**
     * 目的是帮助用户快速定位具体机型，而不是在全量列表里翻

👉 这一步非常关键：
**Model 是“被定位”的对象，不是“随便浏览”的对象。**

---

## 三、分析结果的整体结构（A / B / C / D）

外层仍然是你一直坚持的 **卡片化 + 横向切换**逻辑：

* 多选多个 Model → 外层是 **Model 卡片 Carousel**
* 左右拖动切换不同 Model
* 每个 Model 都有自己的一套分析结果

### Block A：RA 趋势（单 Model，时间维度）

* 内容：

  * 当前 active Model 的 **RA 月度趋势**
  * 默认展示最近 6 个月
* 数据来源：**RA row**
* 目的：

  * 判断该机型 RA 是高是低
  * 是否存在持续上升 / 波动 / 突变

---

### Block B：Top 问题类型（来自 RA DETAIL，Top N）

> ⚠️ 这是你这次明确修正、而且**非常重要的一点**

* **不是 Top1**
* 而是 **Top N（例如 Top 10）**
* 如果数据不足：

  * 有多少展示多少
  * 不强制凑满

#### Block B 的含义

* 在当前筛选条件下：

  * 时间范围
  * Segment
  * ODM
  * 当前 Model
* 从 **RA DETAIL** 中统计：

  * 该机型在这段时间内**报过哪些问题**
  * 哪些问题出现得最多

#### 问题来源字段（RA DETAIL）

你这里说的是「问题类型」，结合你之前给的 detail 表结构，我对你的理解是：

* 主统计维度应是 **问题类型级别字段**
* 比如：

  * `Fault_Category`（最合理、最稳定）
  * 或你们内部确认的“问题分类字段”
* 统计口径是：

  * **问题出现次数**
  * 而不是 RA 值本身

👉 **Block B 的本质是：结构性问题分布，而不是指标计算。**

---

### Block C：AI Summary（解释型，不是决策型）

* 输入信息：

  * Block A：RA 趋势特征
  * Block B：Top 问题类型分布
* 输出内容：

  * 对当前 Model 的 RA 表现做**总结性描述**
  * 不给整改建议
  * 不下结论性指令
* 作用：

  * 帮用户“快速读懂这个机型在干什么”

---

### Block D：多 Model 对比（全局，不跟随卡片）

当用户 **选择多个 Model** 时：

* 出现 **Block D**
* Block D 的职责是：

  * **横向对比不同 Model 的 RA 水平**
* 展示形式：

  * 饼图
  * * 对应数据表

#### Block D 的对比指标

* 指标来源：**RA row**
* 对比内容：

  * 各 Model 的聚合 RA 值
  * 在相同时间 + Segment + ODM 条件下
* share 的计算口径：

  * 由后端统一定义
  * 前端只展示

👉 Block D 是「选了多个机型以后才有意义的对比视角」。

---

## 四、两条数据链路（你现在这个模块里是**同时存在的**）

### 1️⃣ RA row（指标与趋势用）

用于：

* Block A（RA 趋势）
* Block D（多 Model 对比）

字段核心是：

* Claim_month
* Segment
* Supplier_NEW（ODM）
* Model
* RA CLAIM / RA MM

---

### 2️⃣ RA DETAIL（结构与问题用）

**只用于 Block B**

用于：

* 统计某个 Model 在一段时间内：

  * 报过哪些问题
  * 哪些问题最集中（Top N）

这是**指标分析 + 结构诊断并行**的一个典型模块。

---

## 五、这个模块在整体体系里的“角色感”

* IFIR – Model：偏 **市场端失效结果**
* RA – Model：偏 **返修/售后问题结构**
* 两者结合时：

  * IFIR 告诉你“结果严不严重”
  * RA Model + Top 问题告诉你“问题长什么样”

---

## 六、总结一句话（对齐用）

> **RA – Model 模块 = 在明确的 Segment + ODM 业务上下文中，对具体机型进行 RA 趋势分析 + 机型间对比 + Top 问题类型结构分析（Top N），帮助用户理解“这个机型的 RA 为什么是现在这样”。**


---

# RA – Model（Mode）Analysis

**完整工程任务书**

---

## 0. 模块定位与目标

**模块名称**：RA Model Analysis
**核心分析对象**：Model（Mode / 机型）
**前置业务上下文**：Segment → ODM → Model（强引导、级联选择）
**默认趋势窗口**：最近 6 个月

**模块目标**：
在明确的 Segment 与 ODM 业务范围内，对具体机型的 RA 表现进行分析，结合：

* 短期 RA 趋势
* 多机型 RA 对比
* 来自 RA DETAIL 的 Top 问题类型（Top N）

帮助用户理解：
**某个机型的 RA 是否异常、是否持续、问题主要集中在哪些类型上。**

---

## 1. 用户使用流程（User Flow）

1. 进入 **RA → Model Analysis** 页面
2. 选择时间范围（默认最近 6 个月）
3. 选择 **Segment**（必选，第一层）
4. 选择 **ODM**（必选，第二层）
5. 选择 **Model（Mode）**（必选，多选）

   * Model 选择器：

     * 只展示在「Segment + ODM」条件下存在数据的机型
     * 支持 **关键词搜索**
6. 点击 **Analyze**
7. 查看分析结果：

   * 外层 Model 卡片左右滑动切换
   * 每个 Model 对应 A / B / C
   * 多选 Model 时显示 Block D（全局对比）

---

## 2. 页面整体结构

页面自上而下：

1. **Filter Panel（筛选区，级联）**
2. **Analyze 按钮**
3. **Result Area（分析结果区）**

---

## 3. 分析结果区结构（Block A / B / C / D）

### Block A：RA 趋势（单 Model）

* 展示当前 active Model 的 **RA 月度趋势**
* 默认 6 个月
* 折线图
* 数据来源：RA row

---

### Block B：Top 问题类型（Top N）

* 展示当前 active Model 的 **Top 问题类型**
* Top N（例如 Top 10）
* 若不足 N 条：

  * 有多少展示多少
* 数据来源：**RA DETAIL**
* 统计维度：问题类型字段（如 Fault_Category，最终字段由后端固定）

---

### Block C：AI Summary

* 基于：

  * Block A 的趋势特征
  * Block B 的问题分布
* 输出描述性总结
* 不给执行建议，不下整改结论

---

### Block D：多 Model 对比（仅多选 Model 时）

* 展示各 Model 的 **RA 对比**
* 表现形式：

  * 饼图
  * * 对应数据表
* 数据来源：RA row
* Block D 是 **全局块**：

  * 不随 active Model 切换
  * 只与当前筛选条件有关

---

## 4. 数据来源与口径

### 4.1 RA row（趋势 & 对比）

来源：`RA row.xlsx / Export`
用于：

* Block A（RA Trend）
* Block D（多 Model 对比）

字段：

* `Claim_month`
* `Segment`
* `Supplier_NEW`（ODM）
* `Model`
* `RA CLAIM`
* `RA MM`

RA 公式（后端）：
[
RA = \frac{\sum RA_CLAIM}{\sum RA_MM}
]

---

### 4.2 RA DETAIL（Top 问题）

来源：`RA DETAIL.xlsx / Export`
用于：

* Block B（Top 问题类型）

核心字段（示例）：

* `Claim_Month`
* `Segment`
* `Model`
* `Supplier_NEW`
* **问题类型字段**（如 `Fault_Category`）
* Claim 唯一标识（如 Claim_Item_Nbr）

---

## 5. 页面路由

* Path：`/kpi/ra/model-analysis`
* Name：`RaModelAnalysis`
* Page：`RaModelAnalysisPage.vue`

---

## 6. 前端组件拆分

### 6.1 页面容器

`RaModelAnalysisPage.vue`

职责：

* 初始化 options
* 绑定 store
* 触发 analyze
* 组合渲染 Block A / B / C / D

---

### 6.2 筛选区（级联 + 搜索）

`RaModelFilterPanel.vue`

控件：

* MonthRangePicker（默认最近 6 个月）
* SegmentSelect（必选）
* OdmSelect（必选，受 Segment 限制）
* ModelMultiSelect（必选，多选）

  * 支持搜索
  * 候选值受 Segment + ODM 限制
* Analyze Button

规则：

* Segment / ODM / Model 任一未选 → Analyze 禁用
* Analyze 成功后 → 筛选区进入只读变灰态（lockedFilters）

---

### 6.3 外层 Model Carousel

`RaModelResultCarousel.vue`

* 输入：cards, activeIndex
* 输出：indexChange
* 左右拖动切换 Model

---

### 6.4 Block A

`RaTrendChart.vue`

* 输入：trend[]（[{ month, ra }]）

---

### 6.5 Block B（Top 问题表）

`RaTopIssueTable.vue`

* 输入：

  * issues[]（Top N）
* 字段示例：

  * rank
  * issue_type
  * issue_count

---

### 6.6 Block C

`RaAiSummaryPanel.vue`

* 输入：ai_summary

---

### 6.7 Block D

`RaModelPieSummary.vue`

* 输入：

  * modelPie[]
* 展示：

  * 饼图
  * 表格（model / ra / share / ra_claim / ra_mm）

---

## 7. 状态管理（Pinia）

Store：`useRaModelAnalysisStore`

### State

```ts
options {
  segments: string[]
  odms: string[]
  models: string[]
  monthMin
  monthMax
}

filtersDraft {
  startMonth
  endMonth
  segment
  odms: string[]
  models: string[]
}

result {
  cards: RaModelCardData[]
  activeIndex
}

summary {
  modelPie: ModelPieRow[]
}

ui {
  analyzing
  lockedFilters
  error
}
```

---

## 8. API 设计

### 8.1 Options API（级联）

`GET /api/ra/model-analysis/options`

支持参数：

* segment
* odm

用于：

* 根据 Segment → ODM → Model 逐级返回候选

---

### 8.2 Analyze API

`POST /api/ra/model-analysis/analyze`

**Request**

```json
{
  "time_range": { "start_month": "2025-07", "end_month": "2025-12" },
  "filters": {
    "segment": "SEG_A",
    "odms": ["ODM_X"],
    "models": ["MODEL_1", "MODEL_2"]
  },
  "view": {
    "trend_months": 6,
    "top_issue_n": 10
  }
}
```

**Response**

```json
{
  "summary": {
    "model_pie": [
      { "model": "MODEL_1", "ra": 0.02, "share": 0.6 }
    ]
  },
  "cards": [
    {
      "model": "MODEL_1",
      "trend": [{ "month": "2025-07", "ra": 0.018 }],
      "top_issues": [
        { "rank": 1, "issue": "Power Failure", "count": 12 }
      ],
      "ai_summary": "..."
    }
  ]
}
```

---

## 9. 后端聚合逻辑（伪代码）

### Block A（RA Trend）

```sql
GROUP BY Model, Claim_month
```

### Block D（Model 汇总）

```sql
GROUP BY Model
```

### Block B（Top 问题，RA DETAIL）

```sql
GROUP BY Model, Fault_Category
ORDER BY COUNT(*) DESC
LIMIT :topN
```

---

## 10. 关键交互规则

* Segment / ODM 变化 → Model options 级联更新
* 切换 Model 卡片 → A/B/C 同步更新
* Block D 不随卡片切换
* Analyze 后筛选锁定

---

## 11. 验收标准

1. Segment → ODM → Model 强级联生效
2. Model 支持搜索
3. 单 Model 显示 A/B/C
4. 多 Model 显示 Block D
5. Top 问题为 Top N，不强制 Top1
6. RA row 与 RA detail 数据链路清晰分离
7. 前端不计算 RA、不计算 Top 问题

---
